import { NgModule } from '@angular/core';

import { <%= widget_component_name %> } from './widget.component';

@NgModule({
  declarations: [
    <%= widget_component_name %>
  ],
  expotrs: [
    <%= widget_component_name %>
  ]
})
export class <%= widget_module_name %> { }
